{
  "installCommand": "HUSKY=0 bun i && (cd ../../packages/core && HUSKY=0 bun i) && (cd ../../packages/shared && HUSKY=0 bun i) && (cd ../../packages/models && HUSKY=0 bun i) && (cd ../../packages/tools && HUSKY=0 bun i) && (cd ../examples && HUSKY=0 bun i)",
  "buildCommand": "(cd ../../packages/shared && bun run build) && (cd ../../packages/models && bun run build) && (cd ../../packages/tools && bun run build) && (cd ../examples && bun run build) && bun run build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/cleanup",
      "schedule": "0 0 * * *"
    }
  ]
}
