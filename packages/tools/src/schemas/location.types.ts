/**
 * Re-export location types from @lucky/shared for backward compatibility
 */

export type { StandardizedLocation, LocationData, PartialLocationData, WorkflowLocationData } from "@lucky/shared"
export { DataQuality, locationDataSchema, exampleLocationData } from "@lucky/shared"
