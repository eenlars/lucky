/**
 * Re-export tool types from @lucky/shared for backward compatibility
 */

export type { CodeToolName, CodeToolFailure, CodeToolSuccess, CodeToolResult } from "@lucky/shared"
export { Tools as default } from "@lucky/shared"
