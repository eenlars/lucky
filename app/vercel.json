{
  "installCommand": "bun install && cd ../core && bun install && cd ../shared && bun install && bun run build",
  "crons": [
    {
      "path": "/api/cron/cleanup",
      "schedule": "0 0 * * *"
    }
  ]
}