{
  "installCommand": "bun i && cd ../core && bun i && cd ../packages/shared && bun i && cd ../runtime && bun i",
  "buildCommand": "cd ../packages/shared && bun run build && cd ../runtime && bun run build && cd ../app && bun run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/cleanup",
      "schedule": "0 0 * * *"
    }
  ]
}
