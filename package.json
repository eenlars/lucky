{
  "name": "lucky-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "app",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "npm run dev -w app",
    "build": "npm run build -w app",
    "test": "npm run test -w app",
    "cultural": "npm run cultural -w app",
    "once": "npm run once -w app",
    "clean": "npm run clean -w app"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}