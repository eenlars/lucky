{
  "type": "module",
  "workspaces": [
    "app",
    "core",
    "runtime",
    "shared"
  ],
  "dependencies": {
    "@mendable/firecrawl-js": "^1.29.3",
    "nanoid": "^5.1.5",
    "puppeteer": "^24.15.0"
  },
  "devDependencies": {
    "@lucky/core": "workspace:*",
    "@types/papaparse": "^5.3.16",
    "husky": "^9.1.7",
    "tsconfig-paths": "^4.2.0",
    "tsx": "^4.20.3",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "prepare": "husky",
    "test:smoke": "vitest run tests/e2e-essential/smoke --config tests/e2e-essential/vitest.config.ts",
    "test:gate": "vitest run tests/e2e-essential --config tests/e2e-essential/vitest.config.ts",
    "tsc": "cd app && tsc --noEmit && cd ../core && tsc --noEmit && cd ../runtime && tsc --noEmit && cd ../shared && tsc --noEmit"
  }
}
